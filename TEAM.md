# Team

## Members

| Name                         | Student Code | GitHub Username | Role             | Module           | Feature Branch         |
|------------------------------|--------------|---------------| ---------------- | ---------------- | ---------------------- |
| Omar Sebastian Gamez Heredia | 1047039566   | Osgh1015      | Technical Leader | Sale + UI + Main | feature/sale-module    |
| Santiago Andres Herrera Acuña | 1066281586   | Sandres-Herrera | Developer 1      | Product          | feature/product-module |
| Pablo Andres Amaya Pabon     | 1066281972   | PAMAYA22      | Developer 2      | Person           | feature/person-module  |

## Class Distribution

### Technical Leader

* `Sale`
* `SaleRepository`
* `SaleService`
* `ConsoleMenu`
* `Main`

### Developer 1

* `Product` (abstract)
* `VideoGame`
* `Console`
* `ProductRepository`
* `ProductService`

### Developer 2

* `Person` (abstract)
* `Client`
* `Seller`
* `PersonRepository`
* `PersonService`

## Committed Activities

### Technical Leader

1. Create and configure the GitHub repository.
2. Configure the `main` and `develop` branches.
3. Configure the Maven project and the four-layer package structure.
4. Define the team information and class distribution.
5. Implement the `Sale` class.
6. Implement the sale total calculation.
7. Implement `SaleRepository` for file persistence.
8. Implement `SaleService` with the required sale validations.
9. Implement the basic console menu.
10. Implement the product, people, and sales submenus.
11. Implement `Main` with initial data loading and dependency injection.
12. Review and integrate the developers' Pull Requests.
13. Complete the final project documentation and README.

### Developer 1

1. Create the `feature/product-module` branch.
2. Implement the abstract `Product` class and its common attributes and methods.
3. Define the required abstract product behavior.
4. Implement the `VideoGame` class.
5. Implement the `Console` class.
6. Implement `ProductRepository` for saving and loading products.
7. Implement `ProductService` for product registration, listing, and stock management.
8. Add JavaDoc in English to the product module.
9. Open a Pull Request for integration into `develop`.

### Developer 2

1. Create the `feature/person-module` branch.
2. Implement the abstract `Person` class and its common attributes and methods.
3. Define the required behavior for the person subclasses.
4. Implement the `Client` class.
5. Implement the `Seller` class.
6. Implement `PersonRepository` for saving and loading clients and sellers.
7. Implement `PersonService` for registration and listing operations.
8. Add JavaDoc in English to the people module.
9. Open a Pull Request for integration into `develop`.
